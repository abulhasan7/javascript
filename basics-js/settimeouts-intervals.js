//set timeout, do task after some milliseconds
setTimeout(()=>console.log('hey'),2000);
//set interval, do task repeatedly after some milliseconds
setInterval(()=>console.log('hey interval'),2000);

//clear interval, to clear interval